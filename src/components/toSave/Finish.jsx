import Calendar from "./Calendar";

const Finish = () => {
  return (
    <div>
      <section>
        <p>독서기간</p>
        <Calendar />
        <Calendar />
      </section>

      <section>
        <p>평가하기</p>
      </section>
    </div>
  );
};

export default Finish;
